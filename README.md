# 🧬 Flotation Collector Discovery Pipeline

**Статус: Неделя 3 — расширенная библиотека и скрининг.** Виртуальный скрининг молекул-собирателей для сульфидной флотации через связку RDKit + DFT (Psi4) + QSPR, на литературно обоснованном синтетическом таргете. См. [`PLAN.md`](PLAN.md) для полного плана и методологического обоснования.

## Зачем этот проект

Продолжение методологии [`elastomeric_1k_pu`](https://github.com/czeberyak/elastomeric_1k_pu) и [`flotation_reagent_opt`](https://github.com/czeberyak/flotation_reagent_opt) (DoE/RSM/desirability), теперь на уровне **молекулярного дизайна**, а не оптимизации рецептуры/режима на фиксированной химии. Закрывает вычислительный трек (QSPR/QSAR + DFT), упоминаемый как следующий шаг во всём остальном портфолио, но нигде ранее не реализованный даже как методология.

## Класс молекул: алкилксантогенаты

Не произвольный выбор — самый распространённый и наиболее изученный в литературе класс собирателей для сульфидной флотации, с задокументированной структурно-функциональной зависимостью (длина цепи, разветвлённость) и прямым литературным прецедентом использования DFT-дескрипторов граничных орбиталей для предсказания относительной эффективности флотации. Подробности и источники — в `PLAN.md`, раздел 2.

## Что это НЕ

- Не полная периодическая DFT адсорбции молекулы на срезе сульфидной поверхности (это отдельная, значительно более тяжёлая задача).
- Не утверждение об открытии нового реального реагента — демонстрация методологии на правдоподобном синтетическом таргете.
- Не production-инструмент — учебный/демонстрационный MVP по плану на 2–3 недели.

## DFT: методологическая валидация

Ксантогенаты — анионы, и это оказалось методологически значимым, а не формальностью:

- **Базис по умолчанию — `B3LYP/6-31+G*`, не `6-31G*`.** Контрольная точка — воспроизвести ранжирование по энергиям граничных орбиталей amyl > isobutyl > ethyl xanthate из [Yang, Albijanic, Liu & Zhou, 2018](https://doi.org/10.1016/j.mineng.2018.05.032) (*Minerals Engineering*, 125, 155–164), экспериментально подтверждённое там же на золото-пиритовой руде. На `6-31G*` — физически неправдоподобный HOMO (~-0.3 эВ) и порядок, обратный литературному. С диффузными функциями HOMO встал в разумный диапазон (~-0.8 эВ), а ранжирование по HOMO-LUMO gap совпало.
- **Несколько стартовых конформаций, не одна.** Для цепей от C7 одна случайная UFF-геометрия недостаточно надёжно находит глобальный минимум — `smiles_to_xyz_block` сэмплирует несколько конформеров (по умолчанию 10) и берёт лучший по UFF-энергии как старт для DFT-оптимизации.
- **Известное ограничение:** `dipole_debye` в текущих результатах, вероятно, содержит origin-dependent артефакт (дипольный момент заряженной частицы зависит от выбора начала координат) — не физический сигнал в чистом виде. В QSPR-модели Недели 2 требует либо явной перецентровки на центр масс, либо исключения из фичей. `HOMO`/`LUMO`/`gap` этой проблемы не имеют.

## Результаты Недели 1

Полная библиотека (15/15) посчитана на `B3LYP/6-31+G*`, ~55 минут суммарно на CPU ноутбука. Два содержательных сигнала видны уже на сырых DFT-дескрипторах, без QSPR-модели:

- **HOMO-LUMO gap монотонно снижается с длиной прямой цепи** — с 4.01 эВ (methyl) до ~2.4–2.8 эВ (C8–C10), что соответствует более высокой реакционной способности длинноцепочечных ксантогенатов.
- **Разветвлённые изомеры систематически уступают прямоцепочечным** того же числа атомов углерода — во всех 5 парах-сравнениях больший gap и более отрицательный HOMO, что соответствует литературным данным о меньшей эффективности изо-ксантогенатов.

Данные — в `data/03_processed/dft_descriptors.csv`.

## QSPR: методология и результаты

Ridge-регрессия (`src/qspr/model.py`) для двух откликов — recovery-proxy и selectivity-proxy (`src/qspr/target.py`) — от структурных (RDKit: молекулярная масса, LogP, TPSA) и электронных (DFT: HOMO, gap) дескрипторов. Длина цепи и признак `branched` вычисляются из молекулярного графа через RDKit, не из имени кандидата — работает и на структурах, которых код никогда не видел.

- **Главная метрика — leave-one-out CV, не in-sample R².** На 15 точках и 9 признаках in-sample почти гарантированно оптимистичен; честная оценка — LOO: recovery R² = 0.891, selectivity R² = 0.975.
- **`lumo_ev` исключён из фич вместе с `dipole_debye`.** `gap = lumo − homo` — точное тождество, не эмпирическая корреляция; втроём homo/lumo/gap несут только 2 степени свободы и произвольно делят между собой вес коэффициентов.
- **`branched × n_carbons` — отдельная фича.** Без неё линейная модель не может выразить branching-эффект, меняющий знак с длиной цепи (короткие разветвлённые эффективнее прямых аналогов, длинные — наоборот).
- **`branch_distance` — расстояние от точки присоединения до ближайшей развилки.** Молекулярная масса, LogP (Crippen) и TPSA — локально-аддитивные по типам атомов, не различают, ГДЕ на цепи сидит развилка (см. ниже, скрининг). Эта фича закрывает пробел — и оказалась самым весомым признаком для суррогата gap (ниже).
- **`tpsa` — нулевой коэффициент, и это ожидаемо, не баг.** Полярная поверхность одна и та же во всей библиотеке (ксантогенатная голова), алкильный хвост в TPSA вклада не даёт.
- **Известное ограничение:** `2-ethylhexyl` — худшая LOO-точка по recovery (ошибка 0.15). Единственный разветвлённый кандидат длиннее C5 во всей исходной библиотеке — leave-one-out на нём тестирует чистую экстраполяцию в область без единого обучающего примера.

Данные — в `data/03_processed/qspr_dataset.csv`.

## Скрининг расширенной библиотеки (Неделя 3)

Комбинаторный генератор (`src/molecules/expanded_library.py`) — систематический, не исчерпывающий набор одноветочных изомеров C4–C12 (прямые цепи + метил/этил-ветка на разных позициях главной цепи), за вычетом структур, уже покрытых настоящим DFT в исходных 15: 111 сгенерировано, 27 совпало с исходными (исключены), 72 уникальных после RDKit-дедупликации. Суррогат (`src/qspr/dft_surrogate.py`) предсказывает HOMO/gap из одних RDKit-фич, без единого значения реального DFT на входе — LOO R² = 0.868 (gap), заметно ниже точности настоящего расчёта, но достаточно для дешёвого ранжирования; не для доверия топ-1 без проверки. `src/screening/rank_candidates.py` объединяет обе библиотеки в один рейтинг (15 real_dft + 72 surrogate_predicted) с явной пометкой источника gap — не смешивает данные разной надёжности под одной цифрой.

- **Позиция ветки — отдельный признак (`branch_distance`), закрыл почти весь пробел.** Первый прогон показал: молекулярная масса, LogP и TPSA не различают, где на цепи развилка — четыре разных структурных изомера C6 давали идентичный предсказанный gap. Расстояние от точки присоединения до ближайшей развилки подняло LOO R² суррогата gap с 0.778 до 0.868 и разрешило 3 из 4 ранее неразличимых кандидатов.
- **Небольшой честный остаток:** ветки одинаковой удалённости от корня, но разной длины (метил vs этил), всё ещё дают одинаковый предсказанный gap — признак видит "где", не "чем". Затрагивает единичные пары, не общую картину; сознательно не стали закрывать дальше — отдача уже маргинальная.
- Топ-кандидаты из surrogate-части, разветвлённые и длиннее C5, отдельно выводятся как рекомендованные на реальную DFT-валидацию — дешёвый скрининг сам по себе не основание для выводов, только для отбора на дорогую проверку.

Данные — в `data/03_processed/screening_ranked.csv`.

## Стек

RDKit (молекулярные дескрипторы, конформационный поиск) · Psi4 (DFT, `B3LYP/6-31+G*`) · scikit-learn/statsmodels (QSPR) · Streamlit + Plotly (дашборд)

## Статус по компонентам

- [x] Генерация библиотеки кандидатов (`src/molecules/`) — 15 структур, `tests/test_candidate_library.py` 10/10
- [x] DFT-дескрипторы (`src/dft/`) — 15/15 посчитаны, чек-поинт против литературы ([Yang et al., 2018](https://doi.org/10.1016/j.mineng.2018.05.032)) пройден
- [x] QSPR-модель (`src/qspr/`) — ridge на 9 признаках, leave-one-out CV: recovery R²=0.891, selectivity R²=0.975
- [x] Виртуальный скрининг (`src/screening/`) — 15 real_dft + 72 surrogate_predicted (gap LOO R²=0.868) ранжированы
- [ ] Streamlit-дашборд (`app/`)

## Установка

```
conda env create -f environment.yml
conda activate flotation-collectors
python src/molecules/candidate_library.py   # проверка библиотеки кандидатов
python3 -m unittest tests.test_candidate_library -v   # 10/10
python3 -m src.dft.batch_compute            # ~55 мин, резюмируемый прогон
python3 -m src.qspr.featurize                # → data/03_processed/qspr_dataset.csv
python3 -m src.qspr.model                    # R², коэффициенты, LOO-диагностика
python3 -m src.qspr.dft_surrogate             # суррогат HOMO/gap из RDKit-фич
python3 -m src.molecules.expanded_library     # → data/02_interim/expanded_candidates.csv
python3 -m src.screening.rank_candidates      # → data/03_processed/screening_ranked.csv
```
